"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.19.6
// source: Flight.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.FlightServiceDefinition = exports.CloseSessionResult = exports.CloseSessionRequest = exports.GetSessionOptionsResult_SessionOptionsEntry = exports.GetSessionOptionsResult = exports.GetSessionOptionsRequest = exports.SetSessionOptionsResult_ErrorsEntry = exports.SetSessionOptionsResult_Error = exports.SetSessionOptionsResult = exports.SetSessionOptionsRequest_SessionOptionsEntry = exports.SetSessionOptionsRequest = exports.SessionOptionValue_StringListValue = exports.SessionOptionValue = exports.PutResult = exports.FlightData = exports.RenewFlightEndpointRequest = exports.FlightEndpoint = exports.Location = exports.Ticket = exports.CancelFlightInfoResult = exports.CancelFlightInfoRequest = exports.PollInfo = exports.FlightInfo = exports.FlightDescriptor = exports.SchemaResult = exports.Result = exports.Action = exports.Criteria = exports.ActionType = exports.Empty = exports.BasicAuth = exports.HandshakeResponse = exports.HandshakeRequest = exports.CloseSessionResult_Status = exports.SetSessionOptionsResult_ErrorValue = exports.FlightDescriptor_DescriptorType = exports.CancelStatus = exports.protobufPackage = void 0;
exports.cancelStatusFromJSON = cancelStatusFromJSON;
exports.cancelStatusToJSON = cancelStatusToJSON;
exports.flightDescriptor_DescriptorTypeFromJSON = flightDescriptor_DescriptorTypeFromJSON;
exports.flightDescriptor_DescriptorTypeToJSON = flightDescriptor_DescriptorTypeToJSON;
exports.setSessionOptionsResult_ErrorValueFromJSON = setSessionOptionsResult_ErrorValueFromJSON;
exports.setSessionOptionsResult_ErrorValueToJSON = setSessionOptionsResult_ErrorValueToJSON;
exports.closeSessionResult_StatusFromJSON = closeSessionResult_StatusFromJSON;
exports.closeSessionResult_StatusToJSON = closeSessionResult_StatusToJSON;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const timestamp_1 = require("./google/protobuf/timestamp");
exports.protobufPackage = "arrow.flight.protocol";
/**
 * The result of a cancel operation.
 *
 * This is used by CancelFlightInfoResult.status.
 */
var CancelStatus;
(function (CancelStatus) {
    /**
     * CANCEL_STATUS_UNSPECIFIED - The cancellation status is unknown. Servers should avoid using
     * this value (send a NOT_FOUND error if the requested query is
     * not known). Clients can retry the request.
     */
    CancelStatus[CancelStatus["CANCEL_STATUS_UNSPECIFIED"] = 0] = "CANCEL_STATUS_UNSPECIFIED";
    /**
     * CANCEL_STATUS_CANCELLED - The cancellation request is complete. Subsequent requests with
     * the same payload may return CANCELLED or a NOT_FOUND error.
     */
    CancelStatus[CancelStatus["CANCEL_STATUS_CANCELLED"] = 1] = "CANCEL_STATUS_CANCELLED";
    /**
     * CANCEL_STATUS_CANCELLING - The cancellation request is in progress. The client may retry
     * the cancellation request.
     */
    CancelStatus[CancelStatus["CANCEL_STATUS_CANCELLING"] = 2] = "CANCEL_STATUS_CANCELLING";
    /**
     * CANCEL_STATUS_NOT_CANCELLABLE - The query is not cancellable. The client should not retry the
     * cancellation request.
     */
    CancelStatus[CancelStatus["CANCEL_STATUS_NOT_CANCELLABLE"] = 3] = "CANCEL_STATUS_NOT_CANCELLABLE";
    CancelStatus[CancelStatus["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(CancelStatus || (exports.CancelStatus = CancelStatus = {}));
function cancelStatusFromJSON(object) {
    switch (object) {
        case 0:
        case "CANCEL_STATUS_UNSPECIFIED":
            return CancelStatus.CANCEL_STATUS_UNSPECIFIED;
        case 1:
        case "CANCEL_STATUS_CANCELLED":
            return CancelStatus.CANCEL_STATUS_CANCELLED;
        case 2:
        case "CANCEL_STATUS_CANCELLING":
            return CancelStatus.CANCEL_STATUS_CANCELLING;
        case 3:
        case "CANCEL_STATUS_NOT_CANCELLABLE":
            return CancelStatus.CANCEL_STATUS_NOT_CANCELLABLE;
        case -1:
        case "UNRECOGNIZED":
        default:
            return CancelStatus.UNRECOGNIZED;
    }
}
function cancelStatusToJSON(object) {
    switch (object) {
        case CancelStatus.CANCEL_STATUS_UNSPECIFIED:
            return "CANCEL_STATUS_UNSPECIFIED";
        case CancelStatus.CANCEL_STATUS_CANCELLED:
            return "CANCEL_STATUS_CANCELLED";
        case CancelStatus.CANCEL_STATUS_CANCELLING:
            return "CANCEL_STATUS_CANCELLING";
        case CancelStatus.CANCEL_STATUS_NOT_CANCELLABLE:
            return "CANCEL_STATUS_NOT_CANCELLABLE";
        case CancelStatus.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
/** Describes what type of descriptor is defined. */
var FlightDescriptor_DescriptorType;
(function (FlightDescriptor_DescriptorType) {
    /** UNKNOWN - Protobuf pattern, not used. */
    FlightDescriptor_DescriptorType[FlightDescriptor_DescriptorType["UNKNOWN"] = 0] = "UNKNOWN";
    /**
     * PATH - A named path that identifies a dataset. A path is composed of a string
     * or list of strings describing a particular dataset. This is conceptually
     *  similar to a path inside a filesystem.
     */
    FlightDescriptor_DescriptorType[FlightDescriptor_DescriptorType["PATH"] = 1] = "PATH";
    /** CMD - An opaque command to generate a dataset. */
    FlightDescriptor_DescriptorType[FlightDescriptor_DescriptorType["CMD"] = 2] = "CMD";
    FlightDescriptor_DescriptorType[FlightDescriptor_DescriptorType["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(FlightDescriptor_DescriptorType || (exports.FlightDescriptor_DescriptorType = FlightDescriptor_DescriptorType = {}));
function flightDescriptor_DescriptorTypeFromJSON(object) {
    switch (object) {
        case 0:
        case "UNKNOWN":
            return FlightDescriptor_DescriptorType.UNKNOWN;
        case 1:
        case "PATH":
            return FlightDescriptor_DescriptorType.PATH;
        case 2:
        case "CMD":
            return FlightDescriptor_DescriptorType.CMD;
        case -1:
        case "UNRECOGNIZED":
        default:
            return FlightDescriptor_DescriptorType.UNRECOGNIZED;
    }
}
function flightDescriptor_DescriptorTypeToJSON(object) {
    switch (object) {
        case FlightDescriptor_DescriptorType.UNKNOWN:
            return "UNKNOWN";
        case FlightDescriptor_DescriptorType.PATH:
            return "PATH";
        case FlightDescriptor_DescriptorType.CMD:
            return "CMD";
        case FlightDescriptor_DescriptorType.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
var SetSessionOptionsResult_ErrorValue;
(function (SetSessionOptionsResult_ErrorValue) {
    /**
     * UNSPECIFIED - Protobuf deserialization fallback value: The status is unknown or unrecognized.
     * Servers should avoid using this value. The request may be retried by the client.
     */
    SetSessionOptionsResult_ErrorValue[SetSessionOptionsResult_ErrorValue["UNSPECIFIED"] = 0] = "UNSPECIFIED";
    /** INVALID_NAME - The given session option name is invalid. */
    SetSessionOptionsResult_ErrorValue[SetSessionOptionsResult_ErrorValue["INVALID_NAME"] = 1] = "INVALID_NAME";
    /** INVALID_VALUE - The session option value or type is invalid. */
    SetSessionOptionsResult_ErrorValue[SetSessionOptionsResult_ErrorValue["INVALID_VALUE"] = 2] = "INVALID_VALUE";
    /** ERROR - The session option cannot be set. */
    SetSessionOptionsResult_ErrorValue[SetSessionOptionsResult_ErrorValue["ERROR"] = 3] = "ERROR";
    SetSessionOptionsResult_ErrorValue[SetSessionOptionsResult_ErrorValue["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SetSessionOptionsResult_ErrorValue || (exports.SetSessionOptionsResult_ErrorValue = SetSessionOptionsResult_ErrorValue = {}));
function setSessionOptionsResult_ErrorValueFromJSON(object) {
    switch (object) {
        case 0:
        case "UNSPECIFIED":
            return SetSessionOptionsResult_ErrorValue.UNSPECIFIED;
        case 1:
        case "INVALID_NAME":
            return SetSessionOptionsResult_ErrorValue.INVALID_NAME;
        case 2:
        case "INVALID_VALUE":
            return SetSessionOptionsResult_ErrorValue.INVALID_VALUE;
        case 3:
        case "ERROR":
            return SetSessionOptionsResult_ErrorValue.ERROR;
        case -1:
        case "UNRECOGNIZED":
        default:
            return SetSessionOptionsResult_ErrorValue.UNRECOGNIZED;
    }
}
function setSessionOptionsResult_ErrorValueToJSON(object) {
    switch (object) {
        case SetSessionOptionsResult_ErrorValue.UNSPECIFIED:
            return "UNSPECIFIED";
        case SetSessionOptionsResult_ErrorValue.INVALID_NAME:
            return "INVALID_NAME";
        case SetSessionOptionsResult_ErrorValue.INVALID_VALUE:
            return "INVALID_VALUE";
        case SetSessionOptionsResult_ErrorValue.ERROR:
            return "ERROR";
        case SetSessionOptionsResult_ErrorValue.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
var CloseSessionResult_Status;
(function (CloseSessionResult_Status) {
    /**
     * UNSPECIFIED - Protobuf deserialization fallback value: The session close status is unknown or
     * not recognized. Servers should avoid using this value (send a NOT_FOUND error if
     * the requested session is not known or expired). Clients can retry the request.
     */
    CloseSessionResult_Status[CloseSessionResult_Status["UNSPECIFIED"] = 0] = "UNSPECIFIED";
    /**
     * CLOSED - The session close request is complete. Subsequent requests with
     * the same session produce a NOT_FOUND error.
     */
    CloseSessionResult_Status[CloseSessionResult_Status["CLOSED"] = 1] = "CLOSED";
    /**
     * CLOSING - The session close request is in progress. The client may retry
     * the close request.
     */
    CloseSessionResult_Status[CloseSessionResult_Status["CLOSING"] = 2] = "CLOSING";
    /**
     * NOT_CLOSEABLE - The session is not closeable. The client should not retry the
     * close request.
     */
    CloseSessionResult_Status[CloseSessionResult_Status["NOT_CLOSEABLE"] = 3] = "NOT_CLOSEABLE";
    CloseSessionResult_Status[CloseSessionResult_Status["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(CloseSessionResult_Status || (exports.CloseSessionResult_Status = CloseSessionResult_Status = {}));
function closeSessionResult_StatusFromJSON(object) {
    switch (object) {
        case 0:
        case "UNSPECIFIED":
            return CloseSessionResult_Status.UNSPECIFIED;
        case 1:
        case "CLOSED":
            return CloseSessionResult_Status.CLOSED;
        case 2:
        case "CLOSING":
            return CloseSessionResult_Status.CLOSING;
        case 3:
        case "NOT_CLOSEABLE":
            return CloseSessionResult_Status.NOT_CLOSEABLE;
        case -1:
        case "UNRECOGNIZED":
        default:
            return CloseSessionResult_Status.UNRECOGNIZED;
    }
}
function closeSessionResult_StatusToJSON(object) {
    switch (object) {
        case CloseSessionResult_Status.UNSPECIFIED:
            return "UNSPECIFIED";
        case CloseSessionResult_Status.CLOSED:
            return "CLOSED";
        case CloseSessionResult_Status.CLOSING:
            return "CLOSING";
        case CloseSessionResult_Status.NOT_CLOSEABLE:
            return "NOT_CLOSEABLE";
        case CloseSessionResult_Status.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseHandshakeRequest() {
    return { protocolVersion: 0, payload: Buffer.alloc(0) };
}
exports.HandshakeRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.protocolVersion !== 0) {
            writer.uint32(8).uint64(message.protocolVersion);
        }
        if (message.payload.length !== 0) {
            writer.uint32(18).bytes(message.payload);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseHandshakeRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.protocolVersion = longToNumber(reader.uint64());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            protocolVersion: isSet(object.protocolVersion) ? globalThis.Number(object.protocolVersion) : 0,
            payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.protocolVersion !== 0) {
            obj.protocolVersion = Math.round(message.protocolVersion);
        }
        if (message.payload.length !== 0) {
            obj.payload = base64FromBytes(message.payload);
        }
        return obj;
    },
    create(base) {
        return exports.HandshakeRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseHandshakeRequest();
        message.protocolVersion = object.protocolVersion ?? 0;
        message.payload = object.payload ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseHandshakeResponse() {
    return { protocolVersion: 0, payload: Buffer.alloc(0) };
}
exports.HandshakeResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.protocolVersion !== 0) {
            writer.uint32(8).uint64(message.protocolVersion);
        }
        if (message.payload.length !== 0) {
            writer.uint32(18).bytes(message.payload);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseHandshakeResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.protocolVersion = longToNumber(reader.uint64());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            protocolVersion: isSet(object.protocolVersion) ? globalThis.Number(object.protocolVersion) : 0,
            payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.protocolVersion !== 0) {
            obj.protocolVersion = Math.round(message.protocolVersion);
        }
        if (message.payload.length !== 0) {
            obj.payload = base64FromBytes(message.payload);
        }
        return obj;
    },
    create(base) {
        return exports.HandshakeResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseHandshakeResponse();
        message.protocolVersion = object.protocolVersion ?? 0;
        message.payload = object.payload ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseBasicAuth() {
    return { username: "", password: "" };
}
exports.BasicAuth = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.username !== "") {
            writer.uint32(18).string(message.username);
        }
        if (message.password !== "") {
            writer.uint32(26).string(message.password);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBasicAuth();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.username = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.password = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            username: isSet(object.username) ? globalThis.String(object.username) : "",
            password: isSet(object.password) ? globalThis.String(object.password) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.username !== "") {
            obj.username = message.username;
        }
        if (message.password !== "") {
            obj.password = message.password;
        }
        return obj;
    },
    create(base) {
        return exports.BasicAuth.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseBasicAuth();
        message.username = object.username ?? "";
        message.password = object.password ?? "";
        return message;
    },
};
function createBaseEmpty() {
    return {};
}
exports.Empty = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseEmpty();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.Empty.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseEmpty();
        return message;
    },
};
function createBaseActionType() {
    return { type: "", description: "" };
}
exports.ActionType = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.type !== "") {
            writer.uint32(10).string(message.type);
        }
        if (message.description !== "") {
            writer.uint32(18).string(message.description);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseActionType();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.type = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.description = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : "",
            description: isSet(object.description) ? globalThis.String(object.description) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.type !== "") {
            obj.type = message.type;
        }
        if (message.description !== "") {
            obj.description = message.description;
        }
        return obj;
    },
    create(base) {
        return exports.ActionType.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseActionType();
        message.type = object.type ?? "";
        message.description = object.description ?? "";
        return message;
    },
};
function createBaseCriteria() {
    return { expression: Buffer.alloc(0) };
}
exports.Criteria = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.expression.length !== 0) {
            writer.uint32(10).bytes(message.expression);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCriteria();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.expression = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { expression: isSet(object.expression) ? Buffer.from(bytesFromBase64(object.expression)) : Buffer.alloc(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.expression.length !== 0) {
            obj.expression = base64FromBytes(message.expression);
        }
        return obj;
    },
    create(base) {
        return exports.Criteria.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCriteria();
        message.expression = object.expression ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseAction() {
    return { type: "", body: Buffer.alloc(0) };
}
exports.Action = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.type !== "") {
            writer.uint32(10).string(message.type);
        }
        if (message.body.length !== 0) {
            writer.uint32(18).bytes(message.body);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAction();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.type = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.body = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : "",
            body: isSet(object.body) ? Buffer.from(bytesFromBase64(object.body)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.type !== "") {
            obj.type = message.type;
        }
        if (message.body.length !== 0) {
            obj.body = base64FromBytes(message.body);
        }
        return obj;
    },
    create(base) {
        return exports.Action.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAction();
        message.type = object.type ?? "";
        message.body = object.body ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseResult() {
    return { body: Buffer.alloc(0) };
}
exports.Result = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.body.length !== 0) {
            writer.uint32(10).bytes(message.body);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.body = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { body: isSet(object.body) ? Buffer.from(bytesFromBase64(object.body)) : Buffer.alloc(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.body.length !== 0) {
            obj.body = base64FromBytes(message.body);
        }
        return obj;
    },
    create(base) {
        return exports.Result.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseResult();
        message.body = object.body ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseSchemaResult() {
    return { schema: Buffer.alloc(0) };
}
exports.SchemaResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.schema.length !== 0) {
            writer.uint32(10).bytes(message.schema);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSchemaResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.schema = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { schema: isSet(object.schema) ? Buffer.from(bytesFromBase64(object.schema)) : Buffer.alloc(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.schema.length !== 0) {
            obj.schema = base64FromBytes(message.schema);
        }
        return obj;
    },
    create(base) {
        return exports.SchemaResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSchemaResult();
        message.schema = object.schema ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseFlightDescriptor() {
    return { type: 0, cmd: Buffer.alloc(0), path: [] };
}
exports.FlightDescriptor = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.type !== 0) {
            writer.uint32(8).int32(message.type);
        }
        if (message.cmd.length !== 0) {
            writer.uint32(18).bytes(message.cmd);
        }
        for (const v of message.path) {
            writer.uint32(26).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFlightDescriptor();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.type = reader.int32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.cmd = Buffer.from(reader.bytes());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.path.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            type: isSet(object.type) ? flightDescriptor_DescriptorTypeFromJSON(object.type) : 0,
            cmd: isSet(object.cmd) ? Buffer.from(bytesFromBase64(object.cmd)) : Buffer.alloc(0),
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.String(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.type !== 0) {
            obj.type = flightDescriptor_DescriptorTypeToJSON(message.type);
        }
        if (message.cmd.length !== 0) {
            obj.cmd = base64FromBytes(message.cmd);
        }
        if (message.path?.length) {
            obj.path = message.path;
        }
        return obj;
    },
    create(base) {
        return exports.FlightDescriptor.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFlightDescriptor();
        message.type = object.type ?? 0;
        message.cmd = object.cmd ?? Buffer.alloc(0);
        message.path = object.path?.map((e) => e) || [];
        return message;
    },
};
function createBaseFlightInfo() {
    return {
        schema: Buffer.alloc(0),
        flightDescriptor: undefined,
        endpoint: [],
        totalRecords: 0,
        totalBytes: 0,
        ordered: false,
        appMetadata: Buffer.alloc(0),
    };
}
exports.FlightInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.schema.length !== 0) {
            writer.uint32(10).bytes(message.schema);
        }
        if (message.flightDescriptor !== undefined) {
            exports.FlightDescriptor.encode(message.flightDescriptor, writer.uint32(18).fork()).join();
        }
        for (const v of message.endpoint) {
            exports.FlightEndpoint.encode(v, writer.uint32(26).fork()).join();
        }
        if (message.totalRecords !== 0) {
            writer.uint32(32).int64(message.totalRecords);
        }
        if (message.totalBytes !== 0) {
            writer.uint32(40).int64(message.totalBytes);
        }
        if (message.ordered !== false) {
            writer.uint32(48).bool(message.ordered);
        }
        if (message.appMetadata.length !== 0) {
            writer.uint32(58).bytes(message.appMetadata);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFlightInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.schema = Buffer.from(reader.bytes());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.flightDescriptor = exports.FlightDescriptor.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.endpoint.push(exports.FlightEndpoint.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.totalRecords = longToNumber(reader.int64());
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.totalBytes = longToNumber(reader.int64());
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.ordered = reader.bool();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.appMetadata = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            schema: isSet(object.schema) ? Buffer.from(bytesFromBase64(object.schema)) : Buffer.alloc(0),
            flightDescriptor: isSet(object.flightDescriptor) ? exports.FlightDescriptor.fromJSON(object.flightDescriptor) : undefined,
            endpoint: globalThis.Array.isArray(object?.endpoint)
                ? object.endpoint.map((e) => exports.FlightEndpoint.fromJSON(e))
                : [],
            totalRecords: isSet(object.totalRecords) ? globalThis.Number(object.totalRecords) : 0,
            totalBytes: isSet(object.totalBytes) ? globalThis.Number(object.totalBytes) : 0,
            ordered: isSet(object.ordered) ? globalThis.Boolean(object.ordered) : false,
            appMetadata: isSet(object.appMetadata) ? Buffer.from(bytesFromBase64(object.appMetadata)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.schema.length !== 0) {
            obj.schema = base64FromBytes(message.schema);
        }
        if (message.flightDescriptor !== undefined) {
            obj.flightDescriptor = exports.FlightDescriptor.toJSON(message.flightDescriptor);
        }
        if (message.endpoint?.length) {
            obj.endpoint = message.endpoint.map((e) => exports.FlightEndpoint.toJSON(e));
        }
        if (message.totalRecords !== 0) {
            obj.totalRecords = Math.round(message.totalRecords);
        }
        if (message.totalBytes !== 0) {
            obj.totalBytes = Math.round(message.totalBytes);
        }
        if (message.ordered !== false) {
            obj.ordered = message.ordered;
        }
        if (message.appMetadata.length !== 0) {
            obj.appMetadata = base64FromBytes(message.appMetadata);
        }
        return obj;
    },
    create(base) {
        return exports.FlightInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFlightInfo();
        message.schema = object.schema ?? Buffer.alloc(0);
        message.flightDescriptor = (object.flightDescriptor !== undefined && object.flightDescriptor !== null)
            ? exports.FlightDescriptor.fromPartial(object.flightDescriptor)
            : undefined;
        message.endpoint = object.endpoint?.map((e) => exports.FlightEndpoint.fromPartial(e)) || [];
        message.totalRecords = object.totalRecords ?? 0;
        message.totalBytes = object.totalBytes ?? 0;
        message.ordered = object.ordered ?? false;
        message.appMetadata = object.appMetadata ?? Buffer.alloc(0);
        return message;
    },
};
function createBasePollInfo() {
    return { info: undefined, flightDescriptor: undefined, progress: undefined, expirationTime: undefined };
}
exports.PollInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.info !== undefined) {
            exports.FlightInfo.encode(message.info, writer.uint32(10).fork()).join();
        }
        if (message.flightDescriptor !== undefined) {
            exports.FlightDescriptor.encode(message.flightDescriptor, writer.uint32(18).fork()).join();
        }
        if (message.progress !== undefined) {
            writer.uint32(25).double(message.progress);
        }
        if (message.expirationTime !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.expirationTime), writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePollInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.info = exports.FlightInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.flightDescriptor = exports.FlightDescriptor.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 25) {
                        break;
                    }
                    message.progress = reader.double();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.expirationTime = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            info: isSet(object.info) ? exports.FlightInfo.fromJSON(object.info) : undefined,
            flightDescriptor: isSet(object.flightDescriptor) ? exports.FlightDescriptor.fromJSON(object.flightDescriptor) : undefined,
            progress: isSet(object.progress) ? globalThis.Number(object.progress) : undefined,
            expirationTime: isSet(object.expirationTime) ? fromJsonTimestamp(object.expirationTime) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.info !== undefined) {
            obj.info = exports.FlightInfo.toJSON(message.info);
        }
        if (message.flightDescriptor !== undefined) {
            obj.flightDescriptor = exports.FlightDescriptor.toJSON(message.flightDescriptor);
        }
        if (message.progress !== undefined) {
            obj.progress = message.progress;
        }
        if (message.expirationTime !== undefined) {
            obj.expirationTime = message.expirationTime.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.PollInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePollInfo();
        message.info = (object.info !== undefined && object.info !== null)
            ? exports.FlightInfo.fromPartial(object.info)
            : undefined;
        message.flightDescriptor = (object.flightDescriptor !== undefined && object.flightDescriptor !== null)
            ? exports.FlightDescriptor.fromPartial(object.flightDescriptor)
            : undefined;
        message.progress = object.progress ?? undefined;
        message.expirationTime = object.expirationTime ?? undefined;
        return message;
    },
};
function createBaseCancelFlightInfoRequest() {
    return { info: undefined };
}
exports.CancelFlightInfoRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.info !== undefined) {
            exports.FlightInfo.encode(message.info, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCancelFlightInfoRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.info = exports.FlightInfo.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { info: isSet(object.info) ? exports.FlightInfo.fromJSON(object.info) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.info !== undefined) {
            obj.info = exports.FlightInfo.toJSON(message.info);
        }
        return obj;
    },
    create(base) {
        return exports.CancelFlightInfoRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCancelFlightInfoRequest();
        message.info = (object.info !== undefined && object.info !== null)
            ? exports.FlightInfo.fromPartial(object.info)
            : undefined;
        return message;
    },
};
function createBaseCancelFlightInfoResult() {
    return { status: 0 };
}
exports.CancelFlightInfoResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.status !== 0) {
            writer.uint32(8).int32(message.status);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCancelFlightInfoResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { status: isSet(object.status) ? cancelStatusFromJSON(object.status) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.status !== 0) {
            obj.status = cancelStatusToJSON(message.status);
        }
        return obj;
    },
    create(base) {
        return exports.CancelFlightInfoResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCancelFlightInfoResult();
        message.status = object.status ?? 0;
        return message;
    },
};
function createBaseTicket() {
    return { ticket: Buffer.alloc(0) };
}
exports.Ticket = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.ticket.length !== 0) {
            writer.uint32(10).bytes(message.ticket);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTicket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ticket = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { ticket: isSet(object.ticket) ? Buffer.from(bytesFromBase64(object.ticket)) : Buffer.alloc(0) };
    },
    toJSON(message) {
        const obj = {};
        if (message.ticket.length !== 0) {
            obj.ticket = base64FromBytes(message.ticket);
        }
        return obj;
    },
    create(base) {
        return exports.Ticket.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTicket();
        message.ticket = object.ticket ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseLocation() {
    return { uri: "" };
}
exports.Location = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.uri !== "") {
            writer.uint32(10).string(message.uri);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseLocation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.uri = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { uri: isSet(object.uri) ? globalThis.String(object.uri) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.uri !== "") {
            obj.uri = message.uri;
        }
        return obj;
    },
    create(base) {
        return exports.Location.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseLocation();
        message.uri = object.uri ?? "";
        return message;
    },
};
function createBaseFlightEndpoint() {
    return { ticket: undefined, location: [], expirationTime: undefined, appMetadata: Buffer.alloc(0) };
}
exports.FlightEndpoint = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.ticket !== undefined) {
            exports.Ticket.encode(message.ticket, writer.uint32(10).fork()).join();
        }
        for (const v of message.location) {
            exports.Location.encode(v, writer.uint32(18).fork()).join();
        }
        if (message.expirationTime !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.expirationTime), writer.uint32(26).fork()).join();
        }
        if (message.appMetadata.length !== 0) {
            writer.uint32(34).bytes(message.appMetadata);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFlightEndpoint();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ticket = exports.Ticket.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.location.push(exports.Location.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.expirationTime = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.appMetadata = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            ticket: isSet(object.ticket) ? exports.Ticket.fromJSON(object.ticket) : undefined,
            location: globalThis.Array.isArray(object?.location) ? object.location.map((e) => exports.Location.fromJSON(e)) : [],
            expirationTime: isSet(object.expirationTime) ? fromJsonTimestamp(object.expirationTime) : undefined,
            appMetadata: isSet(object.appMetadata) ? Buffer.from(bytesFromBase64(object.appMetadata)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.ticket !== undefined) {
            obj.ticket = exports.Ticket.toJSON(message.ticket);
        }
        if (message.location?.length) {
            obj.location = message.location.map((e) => exports.Location.toJSON(e));
        }
        if (message.expirationTime !== undefined) {
            obj.expirationTime = message.expirationTime.toISOString();
        }
        if (message.appMetadata.length !== 0) {
            obj.appMetadata = base64FromBytes(message.appMetadata);
        }
        return obj;
    },
    create(base) {
        return exports.FlightEndpoint.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFlightEndpoint();
        message.ticket = (object.ticket !== undefined && object.ticket !== null)
            ? exports.Ticket.fromPartial(object.ticket)
            : undefined;
        message.location = object.location?.map((e) => exports.Location.fromPartial(e)) || [];
        message.expirationTime = object.expirationTime ?? undefined;
        message.appMetadata = object.appMetadata ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseRenewFlightEndpointRequest() {
    return { endpoint: undefined };
}
exports.RenewFlightEndpointRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.endpoint !== undefined) {
            exports.FlightEndpoint.encode(message.endpoint, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRenewFlightEndpointRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.endpoint = exports.FlightEndpoint.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { endpoint: isSet(object.endpoint) ? exports.FlightEndpoint.fromJSON(object.endpoint) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.endpoint !== undefined) {
            obj.endpoint = exports.FlightEndpoint.toJSON(message.endpoint);
        }
        return obj;
    },
    create(base) {
        return exports.RenewFlightEndpointRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRenewFlightEndpointRequest();
        message.endpoint = (object.endpoint !== undefined && object.endpoint !== null)
            ? exports.FlightEndpoint.fromPartial(object.endpoint)
            : undefined;
        return message;
    },
};
function createBaseFlightData() {
    return {
        flightDescriptor: undefined,
        dataHeader: Buffer.alloc(0),
        appMetadata: Buffer.alloc(0),
        dataBody: Buffer.alloc(0),
    };
}
exports.FlightData = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.flightDescriptor !== undefined) {
            exports.FlightDescriptor.encode(message.flightDescriptor, writer.uint32(10).fork()).join();
        }
        if (message.dataHeader.length !== 0) {
            writer.uint32(18).bytes(message.dataHeader);
        }
        if (message.appMetadata.length !== 0) {
            writer.uint32(26).bytes(message.appMetadata);
        }
        if (message.dataBody.length !== 0) {
            writer.uint32(8002).bytes(message.dataBody);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFlightData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.flightDescriptor = exports.FlightDescriptor.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.dataHeader = Buffer.from(reader.bytes());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.appMetadata = Buffer.from(reader.bytes());
                    continue;
                }
                case 1000: {
                    if (tag !== 8002) {
                        break;
                    }
                    message.dataBody = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            flightDescriptor: isSet(object.flightDescriptor) ? exports.FlightDescriptor.fromJSON(object.flightDescriptor) : undefined,
            dataHeader: isSet(object.dataHeader) ? Buffer.from(bytesFromBase64(object.dataHeader)) : Buffer.alloc(0),
            appMetadata: isSet(object.appMetadata) ? Buffer.from(bytesFromBase64(object.appMetadata)) : Buffer.alloc(0),
            dataBody: isSet(object.dataBody) ? Buffer.from(bytesFromBase64(object.dataBody)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.flightDescriptor !== undefined) {
            obj.flightDescriptor = exports.FlightDescriptor.toJSON(message.flightDescriptor);
        }
        if (message.dataHeader.length !== 0) {
            obj.dataHeader = base64FromBytes(message.dataHeader);
        }
        if (message.appMetadata.length !== 0) {
            obj.appMetadata = base64FromBytes(message.appMetadata);
        }
        if (message.dataBody.length !== 0) {
            obj.dataBody = base64FromBytes(message.dataBody);
        }
        return obj;
    },
    create(base) {
        return exports.FlightData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFlightData();
        message.flightDescriptor = (object.flightDescriptor !== undefined && object.flightDescriptor !== null)
            ? exports.FlightDescriptor.fromPartial(object.flightDescriptor)
            : undefined;
        message.dataHeader = object.dataHeader ?? Buffer.alloc(0);
        message.appMetadata = object.appMetadata ?? Buffer.alloc(0);
        message.dataBody = object.dataBody ?? Buffer.alloc(0);
        return message;
    },
};
function createBasePutResult() {
    return { appMetadata: Buffer.alloc(0) };
}
exports.PutResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.appMetadata.length !== 0) {
            writer.uint32(10).bytes(message.appMetadata);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePutResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.appMetadata = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            appMetadata: isSet(object.appMetadata) ? Buffer.from(bytesFromBase64(object.appMetadata)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.appMetadata.length !== 0) {
            obj.appMetadata = base64FromBytes(message.appMetadata);
        }
        return obj;
    },
    create(base) {
        return exports.PutResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePutResult();
        message.appMetadata = object.appMetadata ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseSessionOptionValue() {
    return {
        stringValue: undefined,
        boolValue: undefined,
        int64Value: undefined,
        doubleValue: undefined,
        stringListValue: undefined,
    };
}
exports.SessionOptionValue = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.stringValue !== undefined) {
            writer.uint32(10).string(message.stringValue);
        }
        if (message.boolValue !== undefined) {
            writer.uint32(16).bool(message.boolValue);
        }
        if (message.int64Value !== undefined) {
            writer.uint32(25).sfixed64(message.int64Value);
        }
        if (message.doubleValue !== undefined) {
            writer.uint32(33).double(message.doubleValue);
        }
        if (message.stringListValue !== undefined) {
            exports.SessionOptionValue_StringListValue.encode(message.stringListValue, writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSessionOptionValue();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.stringValue = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.boolValue = reader.bool();
                    continue;
                }
                case 3: {
                    if (tag !== 25) {
                        break;
                    }
                    message.int64Value = longToNumber(reader.sfixed64());
                    continue;
                }
                case 4: {
                    if (tag !== 33) {
                        break;
                    }
                    message.doubleValue = reader.double();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.stringListValue = exports.SessionOptionValue_StringListValue.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            stringValue: isSet(object.stringValue) ? globalThis.String(object.stringValue) : undefined,
            boolValue: isSet(object.boolValue) ? globalThis.Boolean(object.boolValue) : undefined,
            int64Value: isSet(object.int64Value) ? globalThis.Number(object.int64Value) : undefined,
            doubleValue: isSet(object.doubleValue) ? globalThis.Number(object.doubleValue) : undefined,
            stringListValue: isSet(object.stringListValue)
                ? exports.SessionOptionValue_StringListValue.fromJSON(object.stringListValue)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.stringValue !== undefined) {
            obj.stringValue = message.stringValue;
        }
        if (message.boolValue !== undefined) {
            obj.boolValue = message.boolValue;
        }
        if (message.int64Value !== undefined) {
            obj.int64Value = Math.round(message.int64Value);
        }
        if (message.doubleValue !== undefined) {
            obj.doubleValue = message.doubleValue;
        }
        if (message.stringListValue !== undefined) {
            obj.stringListValue = exports.SessionOptionValue_StringListValue.toJSON(message.stringListValue);
        }
        return obj;
    },
    create(base) {
        return exports.SessionOptionValue.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSessionOptionValue();
        message.stringValue = object.stringValue ?? undefined;
        message.boolValue = object.boolValue ?? undefined;
        message.int64Value = object.int64Value ?? undefined;
        message.doubleValue = object.doubleValue ?? undefined;
        message.stringListValue = (object.stringListValue !== undefined && object.stringListValue !== null)
            ? exports.SessionOptionValue_StringListValue.fromPartial(object.stringListValue)
            : undefined;
        return message;
    },
};
function createBaseSessionOptionValue_StringListValue() {
    return { values: [] };
}
exports.SessionOptionValue_StringListValue = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.values) {
            writer.uint32(10).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSessionOptionValue_StringListValue();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.values.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            values: globalThis.Array.isArray(object?.values) ? object.values.map((e) => globalThis.String(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.values?.length) {
            obj.values = message.values;
        }
        return obj;
    },
    create(base) {
        return exports.SessionOptionValue_StringListValue.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSessionOptionValue_StringListValue();
        message.values = object.values?.map((e) => e) || [];
        return message;
    },
};
function createBaseSetSessionOptionsRequest() {
    return { sessionOptions: {} };
}
exports.SetSessionOptionsRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        globalThis.Object.entries(message.sessionOptions).forEach(([key, value]) => {
            exports.SetSessionOptionsRequest_SessionOptionsEntry.encode({ key: key, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetSessionOptionsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    const entry1 = exports.SetSessionOptionsRequest_SessionOptionsEntry.decode(reader, reader.uint32());
                    if (entry1.value !== undefined) {
                        message.sessionOptions[entry1.key] = entry1.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sessionOptions: isObject(object.sessionOptions)
                ? globalThis.Object.entries(object.sessionOptions).reduce((acc, [key, value]) => {
                    acc[key] = exports.SessionOptionValue.fromJSON(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionOptions) {
            const entries = globalThis.Object.entries(message.sessionOptions);
            if (entries.length > 0) {
                obj.sessionOptions = {};
                entries.forEach(([k, v]) => {
                    obj.sessionOptions[k] = exports.SessionOptionValue.toJSON(v);
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.SetSessionOptionsRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetSessionOptionsRequest();
        message.sessionOptions = globalThis.Object.entries(object.sessionOptions ?? {})
            .reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = exports.SessionOptionValue.fromPartial(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseSetSessionOptionsRequest_SessionOptionsEntry() {
    return { key: "", value: undefined };
}
exports.SetSessionOptionsRequest_SessionOptionsEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            exports.SessionOptionValue.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetSessionOptionsRequest_SessionOptionsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = exports.SessionOptionValue.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? exports.SessionOptionValue.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = exports.SessionOptionValue.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return exports.SetSessionOptionsRequest_SessionOptionsEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetSessionOptionsRequest_SessionOptionsEntry();
        message.key = object.key ?? "";
        message.value = (object.value !== undefined && object.value !== null)
            ? exports.SessionOptionValue.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBaseSetSessionOptionsResult() {
    return { errors: {} };
}
exports.SetSessionOptionsResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        globalThis.Object.entries(message.errors).forEach(([key, value]) => {
            exports.SetSessionOptionsResult_ErrorsEntry.encode({ key: key, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetSessionOptionsResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    const entry1 = exports.SetSessionOptionsResult_ErrorsEntry.decode(reader, reader.uint32());
                    if (entry1.value !== undefined) {
                        message.errors[entry1.key] = entry1.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            errors: isObject(object.errors)
                ? globalThis.Object.entries(object.errors).reduce((acc, [key, value]) => {
                    acc[key] = exports.SetSessionOptionsResult_Error.fromJSON(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.errors) {
            const entries = globalThis.Object.entries(message.errors);
            if (entries.length > 0) {
                obj.errors = {};
                entries.forEach(([k, v]) => {
                    obj.errors[k] = exports.SetSessionOptionsResult_Error.toJSON(v);
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.SetSessionOptionsResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetSessionOptionsResult();
        message.errors = globalThis.Object.entries(object.errors ?? {})
            .reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = exports.SetSessionOptionsResult_Error.fromPartial(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseSetSessionOptionsResult_Error() {
    return { value: 0 };
}
exports.SetSessionOptionsResult_Error = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.value !== 0) {
            writer.uint32(8).int32(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetSessionOptionsResult_Error();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.value = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { value: isSet(object.value) ? setSessionOptionsResult_ErrorValueFromJSON(object.value) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.value !== 0) {
            obj.value = setSessionOptionsResult_ErrorValueToJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return exports.SetSessionOptionsResult_Error.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetSessionOptionsResult_Error();
        message.value = object.value ?? 0;
        return message;
    },
};
function createBaseSetSessionOptionsResult_ErrorsEntry() {
    return { key: "", value: undefined };
}
exports.SetSessionOptionsResult_ErrorsEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            exports.SetSessionOptionsResult_Error.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetSessionOptionsResult_ErrorsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = exports.SetSessionOptionsResult_Error.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? exports.SetSessionOptionsResult_Error.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = exports.SetSessionOptionsResult_Error.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return exports.SetSessionOptionsResult_ErrorsEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetSessionOptionsResult_ErrorsEntry();
        message.key = object.key ?? "";
        message.value = (object.value !== undefined && object.value !== null)
            ? exports.SetSessionOptionsResult_Error.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBaseGetSessionOptionsRequest() {
    return {};
}
exports.GetSessionOptionsRequest = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetSessionOptionsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.GetSessionOptionsRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseGetSessionOptionsRequest();
        return message;
    },
};
function createBaseGetSessionOptionsResult() {
    return { sessionOptions: {} };
}
exports.GetSessionOptionsResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        globalThis.Object.entries(message.sessionOptions).forEach(([key, value]) => {
            exports.GetSessionOptionsResult_SessionOptionsEntry.encode({ key: key, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetSessionOptionsResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    const entry1 = exports.GetSessionOptionsResult_SessionOptionsEntry.decode(reader, reader.uint32());
                    if (entry1.value !== undefined) {
                        message.sessionOptions[entry1.key] = entry1.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sessionOptions: isObject(object.sessionOptions)
                ? globalThis.Object.entries(object.sessionOptions).reduce((acc, [key, value]) => {
                    acc[key] = exports.SessionOptionValue.fromJSON(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionOptions) {
            const entries = globalThis.Object.entries(message.sessionOptions);
            if (entries.length > 0) {
                obj.sessionOptions = {};
                entries.forEach(([k, v]) => {
                    obj.sessionOptions[k] = exports.SessionOptionValue.toJSON(v);
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.GetSessionOptionsResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetSessionOptionsResult();
        message.sessionOptions = globalThis.Object.entries(object.sessionOptions ?? {})
            .reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = exports.SessionOptionValue.fromPartial(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseGetSessionOptionsResult_SessionOptionsEntry() {
    return { key: "", value: undefined };
}
exports.GetSessionOptionsResult_SessionOptionsEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            exports.SessionOptionValue.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetSessionOptionsResult_SessionOptionsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = exports.SessionOptionValue.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? exports.SessionOptionValue.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = exports.SessionOptionValue.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return exports.GetSessionOptionsResult_SessionOptionsEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetSessionOptionsResult_SessionOptionsEntry();
        message.key = object.key ?? "";
        message.value = (object.value !== undefined && object.value !== null)
            ? exports.SessionOptionValue.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBaseCloseSessionRequest() {
    return {};
}
exports.CloseSessionRequest = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCloseSessionRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.CloseSessionRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseCloseSessionRequest();
        return message;
    },
};
function createBaseCloseSessionResult() {
    return { status: 0 };
}
exports.CloseSessionResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.status !== 0) {
            writer.uint32(8).int32(message.status);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCloseSessionResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { status: isSet(object.status) ? closeSessionResult_StatusFromJSON(object.status) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.status !== 0) {
            obj.status = closeSessionResult_StatusToJSON(message.status);
        }
        return obj;
    },
    create(base) {
        return exports.CloseSessionResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCloseSessionResult();
        message.status = object.status ?? 0;
        return message;
    },
};
exports.FlightServiceDefinition = {
    name: "FlightService",
    fullName: "arrow.flight.protocol.FlightService",
    methods: {
        /**
         * Handshake between client and server. Depending on the server, the
         * handshake may be required to determine the token that should be used for
         * future operations. Both request and response are streams to allow multiple
         * round-trips depending on auth mechanism.
         */
        handshake: {
            name: "Handshake",
            requestType: exports.HandshakeRequest,
            requestStream: true,
            responseType: exports.HandshakeResponse,
            responseStream: true,
            options: {},
        },
        /**
         * Get a list of available streams given a particular criteria. Most flight
         * services will expose one or more streams that are readily available for
         * retrieval. This api allows listing the streams available for
         * consumption. A user can also provide a criteria. The criteria can limit
         * the subset of streams that can be listed via this interface. Each flight
         * service allows its own definition of how to consume criteria.
         */
        listFlights: {
            name: "ListFlights",
            requestType: exports.Criteria,
            requestStream: false,
            responseType: exports.FlightInfo,
            responseStream: true,
            options: {},
        },
        /**
         * For a given FlightDescriptor, get information about how the flight can be
         * consumed. This is a useful interface if the consumer of the interface
         * already can identify the specific flight to consume. This interface can
         * also allow a consumer to generate a flight stream through a specified
         * descriptor. For example, a flight descriptor might be something that
         * includes a SQL statement or a Pickled Python operation that will be
         * executed. In those cases, the descriptor will not be previously available
         * within the list of available streams provided by ListFlights but will be
         * available for consumption for the duration defined by the specific flight
         * service.
         */
        getFlightInfo: {
            name: "GetFlightInfo",
            requestType: exports.FlightDescriptor,
            requestStream: false,
            responseType: exports.FlightInfo,
            responseStream: false,
            options: {},
        },
        /**
         * For a given FlightDescriptor, start a query and get information
         * to poll its execution status. This is a useful interface if the
         * query may be a long-running query. The first PollFlightInfo call
         * should return as quickly as possible. (GetFlightInfo doesn't
         * return until the query is complete.)
         *
         * A client can consume any available results before
         * the query is completed. See PollInfo.info for details.
         *
         * A client can poll the updated query status by calling
         * PollFlightInfo() with PollInfo.flight_descriptor. A server
         * should not respond until the result would be different from last
         * time. That way, the client can "long poll" for updates
         * without constantly making requests. Clients can set a short timeout
         * to avoid blocking calls if desired.
         *
         * A client can't use PollInfo.flight_descriptor after
         * PollInfo.expiration_time passes. A server might not accept the
         * retry descriptor anymore and the query may be cancelled.
         *
         * A client may use the CancelFlightInfo action with
         * PollInfo.info to cancel the running query.
         */
        pollFlightInfo: {
            name: "PollFlightInfo",
            requestType: exports.FlightDescriptor,
            requestStream: false,
            responseType: exports.PollInfo,
            responseStream: false,
            options: {},
        },
        /**
         * For a given FlightDescriptor, get the Schema as described in Schema.fbs::Schema
         * This is used when a consumer needs the Schema of flight stream. Similar to
         * GetFlightInfo this interface may generate a new flight that was not previously
         * available in ListFlights.
         */
        getSchema: {
            name: "GetSchema",
            requestType: exports.FlightDescriptor,
            requestStream: false,
            responseType: exports.SchemaResult,
            responseStream: false,
            options: {},
        },
        /**
         * Retrieve a single stream associated with a particular descriptor
         * associated with the referenced ticket. A Flight can be composed of one or
         * more streams where each stream can be retrieved using a separate opaque
         * ticket that the flight service uses for managing a collection of streams.
         */
        doGet: {
            name: "DoGet",
            requestType: exports.Ticket,
            requestStream: false,
            responseType: exports.FlightData,
            responseStream: true,
            options: {},
        },
        /**
         * Push a stream to the flight service associated with a particular
         * flight stream. This allows a client of a flight service to upload a stream
         * of data. Depending on the particular flight service, a client consumer
         * could be allowed to upload a single stream per descriptor or an unlimited
         * number. In the latter, the service might implement a 'seal' action that
         * can be applied to a descriptor once all streams are uploaded.
         */
        doPut: {
            name: "DoPut",
            requestType: exports.FlightData,
            requestStream: true,
            responseType: exports.PutResult,
            responseStream: true,
            options: {},
        },
        /**
         * Open a bidirectional data channel for a given descriptor. This
         * allows clients to send and receive arbitrary Arrow data and
         * application-specific metadata in a single logical stream. In
         * contrast to DoGet/DoPut, this is more suited for clients
         * offloading computation (rather than storage) to a Flight service.
         */
        doExchange: {
            name: "DoExchange",
            requestType: exports.FlightData,
            requestStream: true,
            responseType: exports.FlightData,
            responseStream: true,
            options: {},
        },
        /**
         * Flight services can support an arbitrary number of simple actions in
         * addition to the possible ListFlights, GetFlightInfo, DoGet, DoPut
         * operations that are potentially available. DoAction allows a flight client
         * to do a specific action against a flight service. An action includes
         * opaque request and response objects that are specific to the type action
         * being undertaken.
         */
        doAction: {
            name: "DoAction",
            requestType: exports.Action,
            requestStream: false,
            responseType: exports.Result,
            responseStream: true,
            options: {},
        },
        /**
         * A flight service exposes all of the available action types that it has
         * along with descriptions. This allows different flight consumers to
         * understand the capabilities of the flight service.
         */
        listActions: {
            name: "ListActions",
            requestType: exports.Empty,
            requestStream: false,
            responseType: exports.ActionType,
            responseStream: true,
            options: {},
        },
    },
};
function bytesFromBase64(b64) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return globalThis.Buffer.from(arr).toString("base64");
}
function toTimestamp(date) {
    const seconds = Math.trunc(date.getTime() / 1_000);
    const nanos = (date.getTime() % 1_000) * 1_000_000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1_000;
    millis += (t.nanos || 0) / 1_000_000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=flight.js.map